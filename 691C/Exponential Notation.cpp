#include<bits/stdc++.h>
using namespace std;
int main()
{
    int i,j=0,bf=0,af=0,lenb=0,lena=a,dot=0;
    string a,b;
    cin>>a;
    for(i=0;a[i]!='\0';i++)
    {
        if(dot==0)
        {
             if(a[i]>'0'&&a[i]<='9')
            {
                bf=1;
                b[j]=a[i];
            }
            if(a[i]=='0')
            {
                if(bf==1)
            }
        }
    }
}
