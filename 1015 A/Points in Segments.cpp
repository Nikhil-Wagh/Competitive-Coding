#include<bits/stdc++.h>
using namespace std;
int main() {
    int n, m, l, r;
    cin >> n >> m;
    vector <pair<int, int> > v;
    for (int i = 0 ; i < n ; i++){
        cin >> l >> r;
        v.push_back(make_pair(l, r));
    }
    sort(v.begin(), v.end());

    for (int i = 0 ; i < n ; i++) {

    }
}
